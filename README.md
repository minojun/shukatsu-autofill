# 就活エントリー自動入力

就活エントリーフォームに、事前登録した個人情報をワンクリックで自動入力する Chrome 拡張機能です。

## 対応プラットフォーム

| システム | 主な利用企業 |
|---|---|
| **i-webs.jp 系** | キオクシア・ルネサス・日立システムズ/ソリューションズ・NECネッツエスアイ・伊藤忠テクノ・NTTデータ（子会社）・野村総研・ソニー・三菱UFJ銀行・三井住友銀行・SCSK など |
| **axol.jp 系** | IBM Japan・TIS・信越化学・東芝・みずほFG・野村證券・KDDI・TDK・富士ソフト・NECグループ など |
| **e2r.jp 系** | 大和証券・三井住友信託銀行 |
| **snar.jp 系** | NTTデータ（本体）・NTTドコモ・NTT東日本・ソフトバンク など |

> **対応外**: 富士通（Workday ATSのためフィールドIDが動的生成）

## インストール

### 開発者モードで読み込む（推奨）

1. このリポジトリを ZIP ダウンロードまたは `git clone`
2. Chrome で `chrome://extensions/` を開く
3. 右上「デベロッパーモード」を ON
4. 「パッケージ化されていない拡張機能を読み込む」→ このフォルダを選択

## 使い方

### 1. 個人情報を登録する

拡張機能アイコンをクリック →「個人情報を編集」から以下を登録して「保存」。

- 氏名（漢字・カナ・ローマ字）
- 生年月日・性別
- 現住所・電話番号
- 休暇中の連絡先（現住所と異なる場合）
- ゼミ・サークル名
- 卒業年月
- メールアドレス

> データは **この PC 内（chrome.storage.local）にのみ保存** されます。外部送信・クラウド同期はしません。

### 2. エントリーフォームで自動入力する

対応サイトのエントリーフォームを開いた状態で拡張機能アイコンをクリック。  
「✓ 対象ページを検出」と表示されたら「自動入力を実行」ボタンを押す。

入力結果（成功件数 / スキップ件数）が表示されたら、内容を確認して送信してください。

### 3. 応募した企業を自動でメモする（応募履歴・企業メモ）

自動入力を実行すると、そのページの企業名を推測して **応募履歴** に自動で記録します。
一覧は「個人情報を編集」（オプション）ページ下部の「応募履歴・企業メモ」で確認できます。

各機能:

- **企業ごとのメモ帳ページ**: 一覧で企業名をクリックすると、その企業専用のメモ帳ページが開きます。
  - **選考ステータス**（未応募／応募済み／書類選考／一次面接／…／内定／お見送り／辞退）
  - **次回予定日**（面接日・締切などのカレンダー入力）
  - **自由メモ**（面接メモ・提出物・志望動機の下書きなど、入力すると自動保存）
- **手動で企業を追加**: 「＋ 企業を追加」から、サイトに登録していない企業のメモも作成できます。
- **CSV コピー**: 一覧をまとめて CSV でコピーし、Excel 等に貼り付けできます。

> 企業名はページ情報からの推測のため、間違っている場合はメモ帳ページで直接修正してください（手修正後は再実行時に上書きされません）。同じフォームで再実行しても重複せず、日時のみ更新されます。
> データはすべて `chrome.storage.local`（この PC 内）にのみ保存され、外部送信されません。

### 4. バックアップ・復元（再インストール対策）

拡張機能を入れ直すと `chrome.storage.local` のデータが消え、個人情報の再入力が必要になります。これを防ぐため、「個人情報を編集」ページの **「バックアップ / 復元」** から、登録済みの**個人情報**と**応募履歴・企業メモ**をまとめて 1 つの JSON ファイルに保存できます。

- **バックアップを保存**: 現在のデータを `shukatsu-autofill-backup-YYYY-MM-DD.json` としてダウンロードします。
- **ファイルから復元**: 保存した JSON を選ぶと、その内容で現在のデータを上書きします。

> 日本語やメモの改行を含むデータを確実に元通り復元するため、形式は CSV ではなく JSON を採用しています。応募履歴を Excel 等で閲覧したい場合は、別途「CSV をコピー」をご利用ください。

## 自動入力される項目

- 氏名（漢字・カナ・ローマ字）
- 生年月日・性別
- 郵便番号・都道府県・住所・電話番号
- 休暇中の連絡先
- ゼミ・サークル名
- 卒業年月
- メールアドレス（確認欄も含む）

> **自動入力されない項目**: 大学名・学部・学科（各サイト独自のコードを使うため）、志望動機・自己PR（自由記述欄）

## プライバシー

入力した個人情報はお使いのPC内（`chrome.storage.local`）にのみ保存され、外部サーバーへの送信・第三者への提供・クラウド同期は一切行いません。

**プライバシーポリシー**: https://minojun.github.io/shukatsu-autofill/privacy.html

## クレジット

- **原作**: [minojun](https://github.com/minojun)
- **改善**: [0x000x7f](https://github.com/0x000x7f)（応募履歴・企業メモ帳機能、対応サイト拡充、UI改善 ほか）

## ライセンス

MIT
